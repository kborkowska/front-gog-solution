export enum Headers {
  MainContent = "GAME OF THE WEEK",
  HiddenButton = "TOTALLY HAVE IMPLEMENTED THAT",
}

export enum OwnershipState {
  Owns = "Owned",
  InCart = "In Cart",
  Available = "Available",
}

export enum StateEntities {
  Games = "games",
}

export enum ActionTypes {
  ChangeGameOwnershipState,
  ClearCart,
}

export enum ButtonSizes {
  Small = "small",
  Medium = "medium",
  Big = "big",
}
